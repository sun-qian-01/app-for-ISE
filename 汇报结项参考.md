《软件工程导论》课程结项汇报提纲

项目名称：学院学生综合服务与党团管理平台  
团队成员：孙谦，田家铭，刘凡硕，林靖涛  
汇报人：（可按实际汇报人调整）  
日期：2026 年 6 月 30 日  

## 一、项目概述

### 1. 项目范围与主要功能

本项目面向学院学生工作、党团事务和院内服务场景，建设一个“学院学生综合服务与党团管理平台”。系统目标是把学生常见事务从分散的线下沟通、表格流转和人工提醒，收敛到统一平台中，形成学生端、管理端、领导看板和移动端演示版相结合的综合服务入口。

当前工作区已经不只是静态原型，而是形成了“需求与设计文档 + Vue 正式前端工程 + Spring Boot 后端 MVP + H2 本地数据库 + 自动化测试 + 部署脚本 + 小程序演示版”的完整课程项目形态。

主要功能包括：

| 功能模块 | 当前实现内容 |
| --- | --- |
| 认证与权限 | 支持学生、班团骨干、管理老师、学院领导、系统管理员等角色；登录后返回用户信息、角色、权限码、菜单和数据范围；前端有路由守卫，后端有接口权限和对象归属校验。 |
| 学生画像 | 学生查看本人档案；管理端查看学生列表和详情；支持标签维护、成长记录、联系方式维护、敏感字段访问约束、学生注册与批量注册演示。 |
| 知识库与智能问答 | 支持知识条目列表、详情页、模板资源下载、问答来源展示；后端支持关键词匹配、低置信度兜底、RAG/Codex-first 问答链路和索引重建接口。 |
| 党团流程 | 支持入党/入团流程定义、学生本人流程进度、阶段材料提交、管理端待办与审核；后端限制学生只能提交本人当前阶段材料。 |
| 通知中心 | 学生端查看通知、通知详情、单条已读和全部已读；管理端支持通知列表和精准通知页面骨架。 |
| 院内申请与审批 | 支持证明、请假、盖章等申请提交；学生可查看详情和撤回；管理老师可查看待审批、审批通过或驳回；后端已用 H2/JDBC 做持久化和事务控制。 |
| 文件上传下载 | 支持 multipart 文件上传和授权下载，已用于党团材料、知识库资源、申请附件等场景。 |
| 荣誉展示 | 学生端和管理端均有荣誉页面，支持个人/集体荣誉展示和基础筛选。 |
| 审计与系统日志 | 管理端有审计日志、系统日志页面；后端具备审计日志接口基础，为敏感字段访问、审批、导入导出等操作留痕预留能力。 |
| 移动端演示 | `mini_app/` 中保留微信小程序学生端演示版，覆盖首页、知识库、党团进度、通知、证明申请等高频入口。 |

### 2. 部署方式简介

项目支持三类运行方式：

1. 正式 Web 前端：`web/` 目录使用 Vue 3 + Vite 开发，运行 `npm install && npm run dev` 本地预览，运行 `npm run build` 生成静态资源。
2. 后端服务：`src/` 目录是 Spring Boot + Maven 工程，运行 `mvn spring-boot:run` 启动，默认端口 `8080`，接口前缀为 `/api/v1`。
3. 部署脚本：`scripts/` 中提供校园网服务器和运行包部署脚本，可构建前后端、部署到 Nginx 与 systemd，并提供启动、停止、状态检查脚本。

辅助演示方式：

- 根目录 `index.html`、`login.html`、`student.html`、`admin.html` 是早期静态原型，可直接打开查看产品结构。
- `mini_app/` 可通过微信开发者工具导入，用于展示移动端学生服务入口。
- 后端内置 `http://localhost:8080/interaction-test.html`，可用于登录、接口调用、文件上传、党团材料提交、申请审批等联调演示。

## 二、功能满足性

### 1. 基础需求完成情况

| 基础需求 | 完成情况 | 说明 |
| --- | --- | --- |
| 多角色登录与权限控制 | 完全实现 | 已实现登录、当前用户、退出、注册、改密等能力；前端按角色控制路由，后端按当前用户校验权限和数据范围。 |
| 学生端首页 | 完全实现 | 展示待办、未读通知、党团阶段、成长摘要、近期通知等聚合信息。 |
| 管理端首页 | 完全实现 | 展示管理统计、通知、审批、学生和运行情况等概览信息。 |
| 学生画像与信息管理 | 部分实现 | 学生本人画像、管理端学生列表、详情、标签、成长记录、批量注册已实现；更完整的 Excel 导入导出和敏感字段审计仍可继续扩展。 |
| 智能问答与政策知识库 | 完全实现核心闭环 | 支持知识条目、详情、模板、问答、来源、低置信度提示；已增加 RAG/Codex-first 问答链路。知识库版本管理和完整后台编辑仍属于后续增强。 |
| 党团事务流程 | 基本实现 | 支持流程定义、学生进度、材料提交、管理审核和待办；复杂重批、撤回、理论自测等可继续扩展。 |
| 精准通知 | 部分实现 | 学生端通知查看、已读、详情已实现；管理端通知页面和创建接口已具备基础能力，邮件/微信/短信真实发送仍为预留或模拟。 |
| 院内申请与审批 | 完全实现核心闭环 | 支持申请创建、详情、撤回、待审批、通过、驳回；状态机和审批记录持久化已接入 H2。 |
| 文件上传下载 | 基本实现 | 后端支持上传和下载，前端已接入知识库上传、党团材料、申请相关流程；正式环境可替换为 NAS/MinIO 等存储。 |
| 奖励荣誉展示 | 部分实现 | 学生端和管理端页面已具备展示与基础管理结构；完整发布/下线/排序规则可继续增强。 |
| 审计日志与系统日志 | 部分实现 | 前端管理入口和后端审计接口已有基础；完整 AOP 审计、前端错误上报、系统事件落库可继续完善。 |
| 小程序端 | 部分实现 | 已有学生端微信小程序演示版，使用本地 mock 数据，尚未接入真实后端。 |
| 部署与交付 | 基本实现 | 已提供本地运行、运行包构建、校园网服务器部署和运行管理脚本。 |

### 2. 基础需求之外的补充功能

- RAG 智能问答增强：支持向量检索、Codex 优先回答、Responses API 降级、关键词匹配兜底。
- 知识库问答流式接口：`POST /api/v1/kb/qa/stream`，便于前端显示问答进度。
- 学生注册、密码修改、联系方式维护、管理端批量注册。
- 学院领导入口修正为可进入管理台，便于展示学院汇总与管理端导航。
- 管理老师数据范围收窄到班级，避免越权查看或审批非本班学生数据。
- 发布包部署脚本和校园网一键部署脚本，降低验收环境部署成本。

## 三、设计理念

### 1. 总体设计思想

系统采用前后端分离的模块化单体架构：

- 前端：Vue 3 + Vite + Vue Router + Pinia + Axios。
- 后端：Java 17 + Spring Boot + Maven + Spring JDBC。
- 数据库：开发和测试使用 H2，SQL 结构向 PostgreSQL/人大金仓 Kingbase 兼容。
- 接口：RESTful JSON API，统一前缀 `/api/v1`，统一响应结构为 `code/message/data/requestId`。
- 权限：RBAC 角色权限 + 数据范围控制，前端只做显示控制，后端做真正安全校验。

模块划分：

| 层次 | 内容 |
| --- | --- |
| 前端页面层 | 登录页、学生端布局、管理端布局、领导看板、403/404、知识条目详情、通知详情。 |
| 前端业务模块 | 仪表盘、知识库、党团流程、通知、申请、画像、荣誉、审计日志、系统日志。 |
| API 封装层 | `web/src/api/modules/` 下按 auth、student、kb、party、notice、application、file、honor、audit 等模块封装。 |
| 后端 Controller | 接收 REST 请求，做参数校验和统一响应。 |
| 后端 Service | 承担业务规则、状态机、权限校验、事务边界。 |
| 数据层 | H2 初始化脚本、数据库设计文档、Kingbase/PostgreSQL 兼容建表脚本。 |

总体结构示意：

```text
学生/老师/领导/管理员
        |
        v
Vue Web 前端 / 小程序演示端
        |
        v
Axios API Client / 路由守卫 / Pinia 登录态
        |
        v
Spring Boot REST API (/api/v1)
        |
        v
认证权限、学生画像、知识库、党团、通知、申请、文件、日志等模块
        |
        v
H2 本地数据库 / Kingbase 兼容数据库设计 / 文件资源
```

### 2. 用户体验设计

学生端设计重点是“少操作、快办理”：

- 移动端优先，入口集中在首页、知识库、党团进度、通知、申请、个人画像。
- 知识库问答展示来源和置信度，避免学生把无来源回答当作正式政策。
- 申请、通知、党团材料等高频操作尽量在单页内完成。

管理端设计重点是“高信息密度、批量处理、可追踪”：

- 使用侧边栏导航组织学生画像、知识库、党团、通知、审批、荣誉、日志等模块。
- 列表页保留筛选、分页、状态标签、空状态、错误态。
- 管理老师查看敏感信息和审批行为强调数据范围和审计意识。

视觉风格上，正式前端继承早期原型中的学院业务系统风格：浅色背景、酒红主色、低饱和卡片、清晰的表格与表单层级。学生端更偏轻量办理，管理端更偏工作台。

### 3. 关键设计决策

1. 选择 Vue 3 + Vite 作为正式前端工程，而不是继续扩展静态 HTML  
   原因是项目已从原型进入工程化阶段，需要路由、状态管理、组件复用、API 封装和构建部署能力。静态原型保留作视觉参考，正式业务在 `web/` 中迭代。

2. 选择 Spring Boot 模块化单体作为后端 MVP  
   课程项目时间有限，模块化单体比微服务更适合快速交付和集中测试，同时仍可按 auth、student、kb、party、notice、application 等业务域划分代码。

3. 本地开发优先使用 H2，正式设计兼容 Kingbase  
   H2 便于本地运行和自动化测试，Kingbase/PostgreSQL 兼容 SQL 保证后续正式数据库迁移时接口契约不变。

4. 申请审批使用后端状态机和事务控制  
   院内申请状态不能由前端直接传任意值，而是通过 create、revoke、approve、reject 等接口触发。状态更新和审批记录写入在同一事务中完成，防止“状态变了但记录没写”。

5. 知识库问答坚持“有来源优先、无依据提示”  
   系统先检索知识条目和附件证据，再调用 RAG/大模型链路。没有可靠来源时返回低置信度提示或“未检索到可靠依据”，避免生成不可靠政策答案。

## 四、进度安排

### 1. 开发阶段划分

| 阶段 | 时间 | 主要工作 | 产出 |
| --- | --- | --- | --- |
| 需求整理与原型阶段 | 2026-04 中下旬 | 整理需求文档，抽取核心角色与业务模块，制作静态 Web 原型。 | `README.md`、`index.html`、`student.html`、`admin.html`、`styles.css`、`scripts/app.js` |
| 设计文档阶段 | 2026-04 下旬至 2026-05 上旬 | 完成总体架构、接口、数据库、权限、字典、前端说明、软件设计文档。 | `docs/`、`database/schema.sql`、`软件设计文档.md/pdf/doc` |
| 移动端演示阶段 | 2026-05 上旬 | 完成微信小程序学生端演示版。 | `mini_app/` |
| 正式前端工程阶段 | 2026-05 中旬 | 初始化 Vue 3 + Vite 工程，搭建路由、登录态、权限守卫、组件体系和 mock 层。 | `web/` 正式前端工程 |
| 后端 MVP 阶段 | 2026-05 中下旬 | 初始化 Spring Boot 后端，实现认证、仪表盘、学生、知识库、党团、通知、申请、文件、字典等接口。 | `src/` 后端工程、H2 初始化数据、交互测试页 |
| 联调与增强阶段 | 2026-05 下旬 | 补齐申请提交、知识库上传、账号注册、画像维护、数据范围、RAG 问答等功能。 | 前后端接口联调、RAG 文档、问题修复记录 |
| 测试与部署阶段 | 2026-06 | 运行后端测试、前端构建、前端问答测试，补充部署脚本和验收材料。 | 测试报告、发布脚本、结项汇报文档 |

### 2. 里程碑与时间节点

| 里程碑 | 完成情况 |
| --- | --- |
| M1：需求和原型可展示 | 已完成 |
| M2：架构、接口、数据库文档可交接 | 已完成 |
| M3：微信小程序学生端演示版 | 已完成 |
| M4：Vue 正式前端工程可构建 | 已完成 |
| M5：Spring Boot 后端 MVP 可运行 | 已完成 |
| M6：核心业务闭环可联调 | 已完成，包括登录、知识库问答、党团材料、通知已读、申请审批、文件上传下载 |
| M7：自动化测试通过 | 已完成，当前后端 104 个测试用例通过，前端构建和问答测试通过 |
| M8：部署脚本与结项材料 | 已完成基础版本 |

### 3. 实际进度 vs 计划进度

计划上，项目最初以 Web 原型和文档为主要交付；实际推进过程中，团队进一步完成了正式前端工程、后端 MVP、数据库初始化、自动化测试、RAG 问答增强和部署脚本，因此最终交付物超过了单纯原型的范围。

偏差主要体现在：

- 后端从“设计计划”推进到了“可运行 MVP”，增加了接口和测试工作量。
- 知识库从普通检索扩展到 RAG 问答，增加了环境变量、向量库、降级链路和测试。
- 部分后台管理能力受时间限制仍处于“核心闭环 + 页面骨架”状态，例如完整知识库版本管理、真实通知渠道、完整日志落库等，适合作为后续迭代。

## 五、团队协作

### 1. 团队角色与分工

| 成员 | 主要分工（按当前仓库成果归纳，可按实际汇报再微调） |
| --- | --- |
| 孙谦 | 项目整体推进、后端接口与联调、RAG 问答增强、测试验证、结项材料整理。 |
| 田家铭 | 部署运行、脚本与环境验证、前后端联调支持、运行文档补充。 |
| 刘凡硕 | 软件设计文档、小程序学生端演示版、需求与设计材料整理。 |
| 林靖涛 | 前端页面完善、测试用例与缺陷验证、汇报材料和演示流程支持。 |

### 2. 协作流程与工具

- 版本控制：使用 Git/GitHub 管理代码，当前分支为 `main`，仓库中保留 commit 历史，如 `feat: improve AI chat performance`、`feat: refine admin scopes and application flows`、`add league process flow` 等。
- 文档协作：使用 Markdown 文档沉淀需求、架构、接口、数据库、前端规范、部署方法、问题记录和开发者日记。
- 任务跟踪：通过 `docs/developer-diary.md` 和 `docs/problems-track/` 记录阶段工作、问题反馈与修复点。
- 沟通方式：围绕功能模块和验收要求进行阶段讨论，先确定需求边界，再分模块实现和验证。
- 自动化验证：后端使用 Maven/Surefire 测试报告，前端使用 Vite build 和 Node 测试脚本。

### 3. 协作亮点与挑战

协作亮点：

- 文档先行，`docs/` 目录形成了架构、接口、数据库、权限、字典、前端规范等开发约束，减少了后期命名和边界反复讨论。
- 原型、前端工程和后端工程并存，既保留早期可视化成果，也支持正式工程继续开发。
- 问题追踪比较明确，例如文章详情页、申请上传入口、老师上传权限、审批列表加载失败等问题都有记录或修复痕迹。

主要挑战：

- 模块多，涉及学生事务、党团、通知、审批、知识库、日志等，时间内需要在“功能完整”和“工程质量”之间取舍。
- 前后端接口不断演进，早期 mock 数据、静态原型和真实后端之间需要多次对齐。
- RAG 问答依赖外部模型、向量库和环境变量，必须设计降级链路，避免演示时因外部服务不可用影响主流程。
- 正式数据库目标为 Kingbase，但本地开发和测试使用 H2，需要保持 SQL 和接口契约的兼容意识。

## 六、开发过程

### 1. 环境搭建与配置

后端环境：

- JDK 17
- Maven
- Spring Boot
- H2 file 模式作为本地开发数据库
- H2 mem 模式作为测试数据库
- 后端配置文件：`src/main/resources/application.yml`
- 初始化脚本：`src/main/resources/db/schema-h2.sql`、`src/main/resources/db/data-h2.sql`

前端环境：

- Node.js/npm
- Vue 3
- Vite
- Vue Router
- Pinia
- Axios
- 前端工程目录：`web/`

部署环境：

- 本地开发：前端 Vite dev server + 后端 Spring Boot。
- 服务器部署：后端 jar + 前端静态资源 + Nginx + systemd。
- 发布脚本：`scripts/make-runtime-release.sh`、`scripts/runtime-install.sh`、`scripts/runtime-start.sh`、`scripts/runtime-status.sh` 等。

大模型辅助情况：

- 使用大模型辅助整理需求文档、生成接口说明、补充测试思路、分析报错、优化代码结构和撰写部分文档。
- 人工重点负责需求取舍、业务规则确认、代码审查、运行验证和最终表述调整。

### 2. 核心功能实现示例

示例一：院内申请与审批状态机

- 学生通过 `POST /api/v1/applications` 创建申请。
- 后端生成申请编号，写入 `biz_application`，同时写入 `biz_approval_record`。
- 学生可在 `submitted/reviewing` 状态撤回。
- 管理老师可在权限和数据范围允许时审批通过或驳回。
- 非法状态流转返回 `40900`。
- 状态更新和审批记录写入在事务中同时成功或回滚。

人工优化点：

- 收窄管理老师数据范围到本班，避免越权审批。
- 修复审批列表 SQL 字段歧义和班级范围过滤问题。
- 增加测试覆盖撤回、审批、越权拦截和事务回滚。

示例二：知识库智能问答

- 前端学生知识库页面支持提问、连续追问、来源展示和低置信度提示。
- 后端 `POST /api/v1/kb/qa` 先检索已发布知识条目，生成候选来源。
- RAG 开启时优先调用 Codex 问答链路，再降级到 Responses API，最后回退关键词匹配。
- 无可靠来源时不编造政策，返回“未检索到可靠依据”或低置信度通用提示。

人工优化点：

- 设计 sources 返回结构，让学生能看到答案依据。
- 设置降级链路，降低外部模型或向量库不可用的演示风险。
- 增加 `FallbackQaAnswerClientTest`、`KbRagServiceTest`、`CodexQaClientTest` 等测试。

### 3. 版本迭代记录

主要迭代节点：

| 节点 | 内容 |
| --- | --- |
| 2026-04-19 | 需求整理、静态 Web 原型、架构和数据库文档初版。 |
| 2026-05-07 | 微信小程序学生端演示版完成。 |
| 2026-05-11 | 软件设计文档整理完成。 |
| 2026-05-14 | Web 静态原型补充申请、荣誉、班团协同、管理端筛选等能力。 |
| 2026-05-18 | Vue 正式前端工程启动，后端 Spring Boot MVP 启动。 |
| 2026-05-20 | 知识库、通知、申请等正式前端业务页继续完善。 |
| 2026-05-27 | 注册、改密、画像维护、批量注册等前端流程增强。 |
| 2026-05-28 | 学生申请提交、知识库上传、管理老师数据范围、学院领导入口修复。 |
| 2026-06 | RAG 问答、部署脚本、测试验证和结项材料补齐。 |

当前 git 最近记录包括：

- `add league process flow`
- `feat:history support and UI improved`
- `feat: improve AI chat performance`
- `fix platform workflow bugs`
- `key word match ai rag`
- `feat: refine admin scopes and application flows`
- `feat: add student registration profile account flows`

## 七、测试过程

### 1. 测试策略

项目采用“自动化测试 + 构建验证 + 交互测试页 + 手动演示”的组合策略。

自动化测试：

- 后端单元测试：覆盖学生、党团、申请、通知、文件、知识库、荣誉、仪表盘、RAG 客户端等服务逻辑。
- 后端集成测试：使用 MockMvc 覆盖登录、核心查询、申请、文件等接口链路。
- 前端测试：使用 `web/tests/kbQa.test.mjs` 验证知识库问答相关前端工具逻辑。
- 前端构建：使用 `npm run build` 验证路由、组件、API 模块和生产构建。

手动测试：

- 通过前端页面验证登录、角色切换、学生端和管理端页面跳转。
- 通过后端交互页验证 token、文件上传、党团材料、学生写接口、申请审批等流程。
- 通过微信开发者工具验证小程序演示版基础页面。

### 2. 测试环境与工具

| 类别 | 工具 |
| --- | --- |
| 后端测试 | JUnit、Spring Boot Test、MockMvc、Mockito、Surefire |
| 后端数据库 | H2 mem 测试数据库 |
| 前端构建 | Vite |
| 前端测试 | Node.js 脚本 |
| 接口调试 | 后端内置 `interaction-test.html`，也可使用 curl/Postman/Apifox |
| 小程序测试 | 微信开发者工具 |
| 版本与状态检查 | Git、Maven、npm |

### 3. 测试用例设计思路

测试用例按“角色、状态、数据范围、异常分支”设计：

1. 角色维度：学生、班团骨干、管理老师、学院领导分别验证可访问页面和接口。
2. 状态维度：申请状态、党团材料状态、通知已读状态、知识库发布状态。
3. 数据范围维度：管理老师只能看本班学生，学院领导可查看更大范围。
4. 异常维度：无权限访问、非法状态流转、无可靠知识库来源、文件下载权限不足。

示例用例：

- 学生提交非当前阶段党团材料，应返回状态冲突。
- 学生撤回他人申请，应返回无权限。
- 管理老师审批非本班学生申请，应被数据范围拦截。
- 知识库无匹配来源时，应返回低置信度或“未检索到可靠依据”。
- 文件上传者和授权用户可下载，未授权用户不可下载。

### 4. 测试执行流程

自动化执行：

```bash
mvn test -q
cd web && npm run build
cd web && npm test
```

手动执行：

1. 启动后端：`mvn spring-boot:run`。
2. 访问 `http://localhost:8080/interaction-test.html`。
3. 使用演示账号登录：
   - 学生：`20220001 / 123456`
   - 班团骨干：`20220018 / 123456`
   - 管理老师：`teacher001 / 123456`
   - 学院领导：`leader001 / 123456`
4. 依次测试登录、知识库问答、党团材料、通知已读、申请创建、撤回、审批、文件上传下载。
5. 前端启动后，通过浏览器验证学生端和管理端页面。

## 八、测试结果

### 1. 测试通过率与关键指标

截至当前工作区状态，已执行验证结果如下：

| 验证项 | 结果 |
| --- | --- |
| 后端 `mvn test -q` | 通过 |
| 后端 Surefire 报告 | 14 个测试报告，104 个测试用例，0 failures，0 errors，0 skipped |
| 前端 `npm run build` | 通过 |
| 前端 `npm test` | 通过 |
| 前端依赖安装 | `npm ci` 成功 |

后端测试覆盖的主要类：

- `ApiIntegrationTest`
- `ApplicationServiceTest`
- `DashboardServiceTest`
- `FileServiceTest`
- `HonorServiceTest`
- `KbServiceTest`
- `PartyServiceTest`
- `StudentServiceTest`
- `NoticeServiceTest`
- `EmbeddingClientTest`
- `LlmResponsesClientTest`
- `FallbackQaAnswerClientTest`
- `KbRagServiceTest`
- `CodexQaClientTest`

### 2. 发现的典型缺陷及修复情况

| 问题 | 修复情况 |
| --- | --- |
| 知识库文章列表只能看列表，不能点进详情 | 已增加知识条目详情路由和详情页。 |
| 页面残留 TODO 注释，影响演示观感 | 已扫描并修复前端展示中的技术性占位痕迹。 |
| 学生端“院内申请”只有查看，没有提交入口 | 已新增提交申请表单，并接入 `POST /api/v1/applications`。 |
| 老师用户无法上传知识库资源 | 已在管理端知识库资源区增加上传表单，并接入文件上传接口。 |
| 管理老师进入审批处理页面加载失败 | 已修复 SQL 字段别名和班级范围过滤逻辑。 |
| 管理老师可见范围过大 | 已将演示管理老师数据范围收窄到本班，并补充后端校验和测试。 |
| 学院领导默认入口不符合管理端演示 | 已调整为默认进入管理台，保留领导看板路由。 |
| RAG 或外部模型不可用会影响问答 | 已设计 Codex -> Responses API -> 关键词匹配的降级链路。 |

### 3. 测试覆盖度评估

功能点覆盖：

- 已覆盖：认证、仪表盘、学生画像、党团流程、通知已读、院内申请、文件、知识库、RAG、荣誉等核心服务逻辑。
- 部分覆盖：管理端通知创建、知识库后台完整版本管理、审计日志完整落库、系统日志前端错误上报。
- 未重点覆盖：真实 Kingbase 环境、真实邮件/微信发送、浏览器端 E2E 自动化、小程序端接口联调。

代码行覆盖率当前未配置 Jacoco 等统计工具，因此本次以测试用例数量、模块覆盖范围和构建结果作为主要评价指标。

### 4. 依赖安全提示

当前执行 `npm ci` 后，npm audit 提示依赖树中存在 2 个 high severity vulnerability。该问题不影响当前构建和课程演示，但建议后续通过 `npm audit` 定位依赖来源，在不破坏 Vite/Vue 版本兼容性的前提下升级依赖。

## 九、工具使用

### 1. 主要使用的大模型工具

项目开发中使用的大模型工具包括：

- OpenAI / ChatGPT / Codex 类工具，用于需求梳理、代码生成、调试分析、测试用例设计和文档撰写。
- 代码补全工具，用于局部代码补全、样式调整和重复结构生成。

### 2. 大模型在各阶段的应用方式

| 阶段 | 应用方式 |
| --- | --- |
| 需求分析 | 辅助从需求文档中提取角色、模块、接口、状态和数据实体。 |
| 架构设计 | 辅助形成前后端分离、模块化单体、RBAC、数据范围、日志体系等设计说明。 |
| 前端开发 | 辅助生成 Vue 页面结构、通用组件、API 封装、表单和状态展示逻辑。 |
| 后端开发 | 辅助实现 Controller、Service、DTO、状态机、异常处理、测试类。 |
| 智能问答 | 辅助设计 RAG 链路、降级策略、环境变量文档和测试用例。 |
| 测试 | 辅助设计边界用例、MockMvc 集成测试、权限和状态冲突测试。 |
| 文档 | 辅助撰写 README、架构文档、接口对齐说明、部署说明、结项汇报材料。 |

### 3. 使用效果总结

效率提升明显的场景：

- 快速从需求文档提取模块和接口，降低前期整理成本。
- 快速生成重复性较高的 Vue 页面、DTO、测试骨架和文档表格。
- 调试 SQL、权限范围、构建错误时能较快定位可能原因。
- 结项材料可基于仓库状态自动归纳，减少人工翻文件的时间。

需要人工修正的场景：

- 大模型容易给出“看起来合理但项目中不存在”的接口或字段，需要对照仓库实际代码核查。
- 对权限、数据范围、状态机这类核心业务规则，必须由人工确认，不能直接照搬生成结果。
- 大模型生成的代码有时重复或过度抽象，需要人工收敛到项目既有风格。
- 文档中涉及团队分工、实际时间节点、验收口径等内容，需要人工结合真实情况最终确认。

## 十、遇到的问题

### 1. 技术与工具问题

1. 前后端工程从静态原型迁移到正式 Vue/Spring Boot 后，早期 mock 数据和真实接口存在字段差异。  
   解决方案：建立 `docs/api.md` 和 `docs/api-mvp-alignment.md`，通过 API 模块封装逐步替换 mock。

2. H2 与 Kingbase/PostgreSQL 存在语法和运行环境差异。  
   解决方案：H2 开启 PostgreSQL 兼容模式，本地先保证测试和演示稳定，数据库设计文档和 `database/schema.sql` 保持 Kingbase 兼容。

3. RAG 链路依赖外部模型、Codex、向量库和环境变量。  
   解决方案：提供 `docs/backend/rag-env-example.md` 和 RAG 脚本，并设计多级降级，不让外部依赖失败影响基础问答。

4. 前端依赖未安装时 `vite: not found`。  
   解决方案：在测试前执行 `cd web && npm ci`，再运行 `npm run build` 和 `npm test`。

5. npm audit 提示高危依赖。  
   解决方案：记录为后续安全治理项，后续评估升级 Vite、插件或间接依赖。

### 2. 团队协作问题

1. 模块多、交付范围容易扩大。  
   解决方案：按 P0/P1/P2 划分，优先保证登录、知识库、党团、通知、申请审批等主流程。

2. 文档、原型、前端、后端并行推进时容易出现口径不一致。  
   解决方案：统一以 `docs/`、`database/schema.sql` 和当前接口实现为基准，并用开发者日记记录阶段变更。

3. 不同成员负责的模块需要集成演示。  
   解决方案：通过统一演示账号、交互测试页和部署脚本降低联调成本。

### 3. 过程管理问题

1. 初期估计更偏向原型交付，后期实际做到了全栈 MVP，工作量增加。  
   经验：课程项目也应尽早明确“原型、MVP、正式系统”的边界。

2. 部分功能做到了页面层，但完整持久化和后台管理还需继续补齐。  
   经验：应优先做能闭环的纵向功能，而不是把所有模块都做成半成品。

3. 测试初期容易被放到最后。  
   经验：后端服务一旦形成状态机或权限逻辑，就应该同步补测试，否则后期修复越权问题成本更高。

### 4. 解决方案与经验教训

- 先文档后实现：接口、字典、角色权限和数据库边界先定下来，后续协作会更稳。
- 先闭环后扩展：申请审批、党团材料、知识库问答等功能先做端到端闭环，再扩展管理能力。
- 前端隐藏不等于安全：后端必须校验权限、数据范围和对象归属。
- 大模型适合加速，但不能替代验收：生成代码后必须运行测试、构建和手动验证。
- 对外部依赖要有降级方案：RAG、邮件、微信等能力都不能成为核心演示的单点风险。

### 5. 其他问题

- 当前仓库中仍保留早期静态原型、正式前端、小程序演示和后端工程，材料较丰富，但也增加了新人理解成本。后续建议在 README 中进一步明确“正式开发主线以 `web/` 和 `src/` 为准，静态原型和小程序为参考/演示”。
- 部分生成文件和测试输出位于 `target/`、`web/dist/` 等目录，提交前需要根据 `.gitignore` 和课程要求决定是否保留。
- 若后续真实部署到学院服务器，应重点补齐 Kingbase 连接、生产环境配置、文件存储目录、HTTPS、日志轮转和数据备份策略。
