# 开发者日记

## 项目名称

学院学生综合服务与党团管理平台

## 记录目的

本文档用于记录当前仓库的开发推进过程、阶段性成果、关键设计决策和后续待办，便于项目成员交接、课程答辩说明和后续继续开发。

## 2026-04-19  CRUEL

### 1. 需求整理与文档转换

本阶段首先处理了原始需求文档，将 `学院学生综合服务与党团管理平台_软件需求规格说明书_已填模板(1).docx` 中的内容提取并整理为 Markdown，写入项目 `README.md`。

本次处理包含：

- 提取文档正文、表格和章节结构
- 将原有软件需求规格说明书改写为仓库内可直接阅读的 Markdown 文档
- 保留需求中的目标、角色、功能模块、性能要求、交付要求与原型范围

产出文件：

- `README.md`

### 2. 初步原型开发

在仓库尚无前后端工程骨架的情况下，为了尽快把需求落成可演示成果，先完成了一版纯静态页面原型。

本阶段实现内容：

- 创建平台入口页
- 创建学生端页面
- 创建管理端页面
- 抽取共享样式文件
- 抽取共享脚本与模拟数据
- 实现基础导航切换、知识库筛选、通知已读、证明申请等前端交互

产出文件：

- `index.html`
- `student.html`
- `admin.html`
- `styles.css`
- `scripts/app.js`

### 3. 首版实现范围

学生端已覆盖：

- 首页聚合视图
- 知识库查询
- 党团进度展示
- 通知中心
- 证明申请记录

管理端已覆盖：

- 业务概览
- 审批处理列表
- 知识库管理视图
- 学生画像视图
- 审计日志视图

### 4. 本阶段决策

- 由于当前仓库没有现成技术栈和依赖安装流程，优先使用原生 HTML、CSS、JavaScript 实现可直接打开运行的静态原型
- 原型重点放在信息架构、业务边界和交互演示，不提前绑定具体后端框架
- 将学生端和管理端拆分为独立页面，便于后续迁移为前后端分层架构

## 2026-04-19  CRUEL

### 1. 总体设计整理

为了让后续正式开发更稳，围绕当前需求和原型补充了总体设计说明，明确了业务域拆分、核心实体、状态设计和数据范围控制。

本阶段梳理内容：

- 用户与权限域
- 学生档案与画像域
- 知识库域
- 通知推送域
- 党团流程域
- 证明申请审批域
- 审计日志域

产出文件：

- `docs/architecture.md`

### 2. 数据库表结构设计

围绕首期功能设计了一版 Kingbase / PostgreSQL 兼容的数据库表结构草案，确保后续开发能够围绕统一数据模型推进。

设计内容包括：

- 用户、角色、权限及关联表
- 学生基础档案、扩展信息、标签、异动记录
- 知识库分类、文章、附件
- 通知主表、通知范围、通知接收状态
- 党团进度与节点记录
- 证明模板、证明申请、审批记录
- 审计日志

产出文件：

- `database/schema.sql`

### 3. 接口文档设计

为配合后续前后端协作，补充了一版 REST 接口文档草案，统一认证方式、响应格式、分页结构与主要接口边界。

本阶段覆盖接口：

- 认证与当前用户
- 学生档案与画像
- 知识库查询与管理
- 通知中心与通知管理
- 党团流程查询与更新
- 证明申请与审批
- 标签管理
- 审计日志
- 学生端与管理端首页聚合接口

产出文件：

- `docs/api.md`

### 4. README 更新

在项目 `README.md` 中补充了后端设计文档入口，便于从仓库首页直接定位总体设计、数据库表结构和接口文档。

## 当前项目状态

当前项目已经具备以下基础：

- 一份完整的需求规格说明书 Markdown 版本
- 一版可直接演示的静态原型
- 一份总体设计草案
- 一份核心数据库表结构草案
- 一份 REST 接口文档草案

当前仍未完成的部分：

- 未建立真实后端工程
- 未接入真实数据库
- 未完成认证、RBAC、审批流等实际服务实现
- 未编写初始化数据、测试用例和部署脚本

## 已有文件清单

- `README.md`
- `index.html`
- `student.html`
- `admin.html`
- `styles.css`
- `scripts/app.js`
- `docs/architecture.md`
- `docs/api.md`
- `database/schema.sql`

## 后续建议

建议后续按以下顺序推进：

1. 选择后端技术栈并初始化工程骨架
2. 将 `database/schema.sql` 导入 Kingbase 进行实际验证
3. 按 `docs/api.md` 实现认证、学生、知识库、通知、证明审批等核心模块
4. 将当前静态原型改造为真正消费接口的数据页面
5. 补充初始化数据脚本、权限种子数据和测试文档

## 备注

本日记记录的是当前仓库内已经完成的开发动作和设计输出，后续每次新增模块、调整结构或修订接口时，建议继续追加记录，而不是覆盖历史内容。

## 2026-05-12  CRUEL

### 1. 基于设计文档回看前端原型

本轮开发前，先对照以下文档重新检查了现有 Web 静态原型：

- `docs/architecture.md`
- `docs/api.md`
- `docs/database.md`
- `docs/frontend-spec.md`
- `docs/role-permission.md`
- `docs/dictionary.md`

检查结论：

- 当前应继续完善 Web 前端原型，而不是小程序方向。
- 现有页面已具备基础信息架构，但仍缺少登录入口、系统日志视图、权限上下文展示等正式开发必需内容。
- 部分前端状态值、类型值和模块命名仍偏原型化，需要向统一字典和接口命名收敛。

### 2. 登录入口补充

为贴合 `docs/frontend-spec.md` 中的公共路由设计，本轮新增了登录页原型，作为前端正式开发前的统一入口。

本阶段新增内容：

- 新建 `login.html`
- 增加 `/auth/login` 和 `/auth/me` 的展示语义
- 增加“模拟角色”切换，便于演示不同角色进入对应工作台

产出文件：

- `login.html`

### 3. 入口页与页面语义调整

对平台入口页和学生端、管理端页面的说明文案进行了调整，使其更贴近“前后端联调原型”的定位，而不是单纯展示型页面。

本阶段修改内容：

- 在 `index.html` 中补充登录入口
- 增加正式开发提示，说明后续应使用真实 API 替换模拟数据
- 在学生端和管理端增加“当前访问上下文”区域
- 明确展示角色代码、默认数据范围和首页对应接口

涉及文件：

- `index.html`
- `student.html`
- `admin.html`

### 4. 学生端页面完善

本轮对学生端页面做了与设计文档更一致的补充，重点是权限提示、问答来源要求和个人档案的展示边界。

完善内容包括：

- 增加当前访问上下文展示
- 增加问答来源说明，强调无可靠依据时不得编造答案
- 增加敏感字段脱敏说明
- 保持学生只能查看本人信息的页面语义

涉及文件：

- `student.html`
- `scripts/app.js`

### 5. 管理端页面完善

本轮对管理端页面做了较大补充，使其更接近 `docs/frontend-spec.md` 中的管理端结构。

新增或完善内容：

- 增加“系统日志”导航与视图
- 增加敏感字段查看约束提示
- 增加当前角色和数据范围说明
- 保持审批、通知、党团流程、学生画像等模块的管理语义

涉及文件：

- `admin.html`
- `scripts/app.js`

### 6. 静态脚本口径统一

本轮对前端静态脚本中的模拟数据进行了统一整理，使其尽量贴近接口文档和字典文档中的命名。

本次统一内容包括：

- 将知识库分类口径向 `scholarship`、`student_status`、`party_league`、`certificate`、`employment` 靠拢
- 将通知渠道口径向 `site`、`email`、`wechat` 靠拢
- 将党团阶段代码向 `applicant`、`activist`、`development_candidate`、`probationary_party_member`、`party_member` 靠拢
- 将申请状态、审批状态、材料审核状态调整为更接近接口和字典的命名
- 增加系统日志模拟数据
- 增加登录页模拟跳转逻辑

涉及文件：

- `scripts/app.js`

### 7. 样式补充

为了支撑新增登录页、权限上下文卡片和系统日志视图，本轮补充了原型样式。

新增样式内容：

- 登录页布局
- 权限上下文卡片
- 系统日志详情块
- 新增说明区和辅助提示文本

涉及文件：

- `styles.css`

### 8. 本轮验证

本轮完成后进行了基础检查：

- 确认 `index.html`、`login.html`、`student.html`、`admin.html` 都正确引用 `styles.css` 和 `scripts/app.js`
- 使用 `node --check` 校验 `scripts/app.js` 语法通过
- 检查新增“系统日志”“敏感字段”“当前访问上下文”等关键内容已经写入页面

### 9. 当前状态

截至本轮，前端静态原型已从“展示型页面”进一步调整为“前后端联调前的结构化原型”，具备以下特征：

- 有统一登录入口
- 页面展示更贴近角色权限设计
- 命名更贴近接口与字典
- 管理端补齐系统日志视图
- 学生端和管理端都强化了权限边界提示

### 10. 后续建议

下一步建议优先进入正式前端工程搭建阶段，而不是长期继续堆叠静态页面。推荐顺序如下：

1. 选定 Vue 3 或 React 技术栈并初始化正式工程
2. 建立路由、鉴权、API client、字典管理和权限控制基础设施
3. 将当前静态原型页面迁移为真实组件页面
4. 用 `docs/api.md` 中的接口逐步替换 `scripts/app.js` 中的模拟数据

## 2026-05-07  刘凡硕

### 1. 微信小程序学生端初版完成

本阶段在 `mini_app` 目录下完成了项目微信小程序版本的初步实现，作为学生端移动端演示版本。该版本基于前期需求文档、总体设计和静态网页原型进行迁移与收敛，重点覆盖学生在移动端的高频业务入口。

本阶段实现内容：

- 初始化微信小程序工程结构
- 配置小程序页面路由、窗口样式和底部 Tab 导航
- 创建首页、知识库、党团进度、通知中心、证明申请 5 个学生端页面
- 抽取小程序全局样式与页面级样式
- 使用 `utils/mock.js` 维护演示数据
- 补充 `mini_app/README.md`，说明目录结构、运行方式和当前版本边界

产出目录与文件：

- `mini_app/README.md`
- `mini_app/app.js`
- `mini_app/app.json`
- `mini_app/app.wxss`
- `mini_app/project.config.json`
- `mini_app/sitemap.json`
- `mini_app/utils/mock.js`
- `mini_app/pages/home/`
- `mini_app/pages/kb/`
- `mini_app/pages/progress/`
- `mini_app/pages/notice/`
- `mini_app/pages/certificate/`

### 2. 小程序功能范围

小程序学生端已覆盖：

- 首页聚合：展示学生基础信息、待办数量、未读通知、快捷入口和近期通知
- 知识库：支持关键词检索和分类筛选
- 党团进度：展示当前阶段、处理状态和流程时间线
- 通知中心：支持通知查看、单条已读和全部已读
- 证明申请：支持选择证明模板、填写用途、提交申请和撤回申请

### 3. 本阶段决策

- 小程序首版定位为学生端移动演示版，优先保证核心业务流程可在微信开发者工具中编译和演示
- 当前仍使用本地模拟数据，暂不接入真实后端接口，便于先验证页面结构和交互流程
- 页面划分沿用学生端业务模块，保持与 `docs/api.md` 中后续接口拆分方向一致
- 管理端暂未迁移到小程序，仍以网页原型和后续后台系统为主要承载形式

### 4. 截至 2026-05-07 的项目状态

当前项目已经具备以下基础：

- 一份完整的需求规格说明书 Markdown 版本
- 一版可直接演示的静态网页原型
- 一份总体设计草案
- 一份核心数据库表结构草案
- 一份 REST 接口文档草案
- 一版微信小程序学生端演示版本

当前仍未完成的部分：

- 未建立真实后端工程
- 未接入真实数据库
- 小程序仍使用本地模拟数据
- 未完成认证、RBAC、审批流等实际服务实现
- 未完成小程序与后端 API 的联调
- 未编写初始化数据、测试用例和部署脚本

### 5. 后续建议

建议后续按以下顺序推进：

1. 使用微信开发者工具导入 `mini_app`，完成页面编译与基础交互检查
2. 选择后端技术栈并初始化工程骨架
3. 将 `database/schema.sql` 导入 Kingbase 进行实际验证
4. 按 `docs/api.md` 实现认证、学生、知识库、通知、证明审批等核心模块
5. 将 `mini_app/utils/mock.js` 中的模拟数据逐步替换为真实接口调用
6. 补充初始化数据脚本、权限种子数据、接口测试和小程序端测试说明
