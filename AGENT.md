# AGENT.md (Local Mandatory Rules)

> 目的：在新会话中给 AI Agent 提供“强制约束”。本文件强调可执行的软件工程规范，默认优先级高于一般建议。

## 1. 开工前必须执行

1. 先阅读并对齐：`README.md`、`docs/architecture.md`、`docs/api.md`、`database/schema.sql`。
2. 先输出 3-7 步执行计划，再开始改代码。
3. 需求不清时，先列出“默认假设”，不要在关键点上静默猜测。

## 2. 实现流程（强制）

1. 先写测试或至少先写可验证的验收用例（TDD 优先）。
2. 小步提交：一次只做一个明确目标，避免超大改动。
3. 每个行为变更都必须同步更新文档（接口/设计/README 中至少一处）。
4. 公共函数、复杂分支、状态机转换必须有注释，注释说明“为什么”，不是“在做什么”。

## 3. 质量门禁（必须通过才可宣称完成）

1. 必须运行并通过相关测试（单元测试至少覆盖新增/改动路径）。
2. 必须运行静态检查（lint/类型检查/格式化）并修复错误。
3. 必须给出验证证据：执行了什么命令、验证了什么结果。
4. 未验证完成前，禁止说“已完成/已修复”。

## 4. 调试与修复规则

1. 先复现问题，再定位根因，再修改，再回归验证。
2. 禁止“猜测式修复”连发；每次改动都要说明对应假设。
3. 出现回归风险时，先补测试再修。

## 5. Git 与协作规则

1. 不得回滚/覆盖他人已改内容，除非明确被要求。
2. 禁止使用破坏性命令（如 `git reset --hard`）处理冲突。
3. 提交说明必须包含：变更点、影响范围、验证方式、剩余风险。

## 6. 安全与合规底线

1. 不在日志、注释、示例中泄露密码、token、身份证号、完整手机号等敏感信息。
2. 涉及权限、审批、状态流转的接口改动，必须补充审计/日志影响说明。
3. 对外接口保持向后兼容；若必须破坏兼容，先写迁移说明。

## 7. 针对本仓库的附加约束

1. 后端接口统一遵循 `docs/api.md` 的响应结构：`code/message/data/requestId`。
2. 数据库设计与枚举必须对齐：`database/schema.sql`、`docs/dictionary.md`。
3. 权限模型必须执行 RBAC + 数据范围，不得只靠前端隐藏按钮。
4. 关键流程（党团流程、审批流程）必须用状态机思维实现，非法状态跳转返回冲突错误。

## 8. 任务收尾模板（每次输出都应包含）

1. 改了什么（文件列表 + 关键点）
2. 为什么这样改（设计理由）
3. 如何验证（命令与结果）
4. 风险与后续建议（如有）

